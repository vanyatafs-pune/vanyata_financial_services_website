'use client'
import { cn } from '@/lib/utils'

// Deprecated: retained as a no-op wrapper so any lingering imports continue to work,
// but visually renders nothing (all section labels have been removed globally).
export function Eyebrow({ children, className }) {
  return null
}

export default Eyebrow
